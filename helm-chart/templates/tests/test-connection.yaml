apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-helloworld.fullname" . }}-test-connection"
  labels:
    {{- include "helm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-helloworld.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
